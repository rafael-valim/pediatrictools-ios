# Deliverfile — configuration for `fastlane deliver`

# App metadata
app_identifier("com.RV.pediatrictools.app")
team_id("CJXZNY36RV")

# Metadata and screenshots paths
metadata_path("./fastlane/metadata")
screenshots_path("./fastlane/screenshots")

# Submission options
submit_for_review(false)
automatic_release(false)
phased_release(true)

# Skip binary upload when only updating metadata
skip_binary_upload(true)

# Skip precheck (IAP validation requires Apple ID login, not API key)
run_precheck_before_submit(false)

# Category, price tier, rating, and copyright are set manually in App Store Connect
# (Fastlane's App Store Connect API has compatibility issues with these fields)
